name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            goos: darwin
            goarch: arm64
            archive: tar.gz
          - os: macos-15-intel
            goos: darwin
            goarch: amd64
            archive: tar.gz
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            archive: tar.gz
          - os: windows-latest
            goos: windows
            goarch: amd64
            archive: zip
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Configure MinGW GCC (Windows only)
        if: runner.os == 'Windows'
        run: echo "C:\Strawberry\c\bin" >> $env:GITHUB_PATH

      - name: Pre-fetch modules (so CGO_CFLAGS path resolution has something to resolve)
        run: go mod download
        shell: bash

      - name: Build
        shell: bash
        env:
          CGO_ENABLED: 1
        run: |
          # Same fix as ci.yml/Makefile (see docs/DESIGN.md, "Windows CI
          # cgo toolchain"): sqlite-vec-go-bindings needs sqlite3ext.h,
          # which only mattn/go-sqlite3 vendors. This workflow calls `go
          # build` directly rather than through `make build`, so it needs
          # the same CGO_CFLAGS fix applied inline rather than inheriting
          # it from the Makefile.
          export CGO_CFLAGS="-I$(go list -m -f '{{.Dir}}' github.com/mattn/go-sqlite3 | tr '\\' '/')"
          VERSION="${GITHUB_REF_NAME#v}"
          COMMIT="$(git rev-parse --short HEAD)"
          DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          EXT=""
          [ "${{ matrix.goos }}" = "windows" ] && EXT=".exe"
          LDFLAGS="-X main.version=$VERSION -X main.commit=$COMMIT -X main.date=$DATE"
          go build -ldflags "$LDFLAGS" -o "hivemindd${EXT}" ./cmd/hivemindd
          go build -ldflags "$LDFLAGS" -o "hivemind${EXT}" ./cmd/hivemind

      - name: Archive (tar.gz)
        if: matrix.archive == 'tar.gz'
        run: tar -czf "hivemind_${{ matrix.goos }}_${{ matrix.goarch }}.tar.gz" hivemindd hivemind

      - name: Archive (zip)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: Compress-Archive -Path hivemindd.exe, hivemind.exe -DestinationPath "hivemind_${{ matrix.goos }}_${{ matrix.goarch }}.zip"

      - uses: actions/upload-artifact@v4
        with:
          name: hivemind_${{ matrix.goos }}_${{ matrix.goarch }}
          path: hivemind_${{ matrix.goos }}_${{ matrix.goarch }}.*
          if-no-files-found: error

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Mint a scoped tap/bucket access token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          owner: causewayai
          repositories: homebrew-causewayai,scoop-causewayai

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Checksums
        working-directory: dist
        run: sha256sum hivemind_* > checksums.txt

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${GITHUB_REF_NAME}" dist/* \
            --title "${GITHUB_REF_NAME}" \
            --generate-notes

      - name: Compute per-asset checksums for tap/bucket
        working-directory: dist
        run: |
          echo "DARWIN_ARM64_SHA=$(grep darwin_arm64 checksums.txt | awk '{print $1}')" >> "$GITHUB_ENV"
          echo "DARWIN_AMD64_SHA=$(grep darwin_amd64 checksums.txt | awk '{print $1}')" >> "$GITHUB_ENV"
          echo "LINUX_AMD64_SHA=$(grep linux_amd64 checksums.txt | awk '{print $1}')" >> "$GITHUB_ENV"
          echo "WINDOWS_AMD64_SHA=$(grep windows_amd64 checksums.txt | awk '{print $1}')" >> "$GITHUB_ENV"

      - name: Update Homebrew tap
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          git clone "https://x-access-token:${GH_TOKEN}@github.com/causewayai/homebrew-causewayai.git" tap
          cat > tap/Formula/hivemind.rb <<EOF
          class Hivemind < Formula
            desc "Local persistent memory for AI harnesses: hivemindd daemon + hivemind CLI"
            homepage "https://github.com/causewayai/hivemind"
            version "${VERSION}"
            license "UNLICENSED"

            on_macos do
              on_arm do
                url "https://github.com/causewayai/hivemind/releases/download/${GITHUB_REF_NAME}/hivemind_darwin_arm64.tar.gz"
                sha256 "${DARWIN_ARM64_SHA}"
              end
              on_intel do
                url "https://github.com/causewayai/hivemind/releases/download/${GITHUB_REF_NAME}/hivemind_darwin_amd64.tar.gz"
                sha256 "${DARWIN_AMD64_SHA}"
              end
            end

            on_linux do
              on_intel do
                url "https://github.com/causewayai/hivemind/releases/download/${GITHUB_REF_NAME}/hivemind_linux_amd64.tar.gz"
                sha256 "${LINUX_AMD64_SHA}"
              end
            end

            def install
              bin.install "hivemindd"
              bin.install "hivemind"
            end

            service do
              run [opt_bin/"hivemindd"]
              keep_alive true
              log_path var/"log/hivemindd.log"
              error_log_path var/"log/hivemindd.log"
              working_dir var
            end

            test do
              assert_match version.to_s, shell_output("#{bin}/hivemindd --version")
              assert_match version.to_s, shell_output("#{bin}/hivemind version")
            end
          end
          EOF
          cd tap
          git config user.name "causeway-release-bot"
          git config user.email "actions@github.com"
          # The formula was renamed hivemindd.rb -> hivemind.rb (it now ships
          # both binaries). Drop the old file on the first release that carries
          # this change; a no-op afterward.
          git rm -f Formula/hivemindd.rb 2>/dev/null || true
          git add Formula/hivemind.rb
          git commit -m "hivemind ${VERSION}"
          git push

      - name: Update Scoop bucket
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          git clone "https://x-access-token:${GH_TOKEN}@github.com/causewayai/scoop-causewayai.git" bucket
          cat > bucket/bucket/hivemind.json <<EOF
          {
            "version": "${VERSION}",
            "description": "Local persistent memory for AI harnesses: hivemindd daemon + hivemind CLI",
            "homepage": "https://github.com/causewayai/hivemind",
            "license": "UNLICENSED",
            "architecture": {
              "64bit": {
                "url": "https://github.com/causewayai/hivemind/releases/download/${GITHUB_REF_NAME}/hivemind_windows_amd64.zip",
                "hash": "${WINDOWS_AMD64_SHA}"
              }
            },
            "bin": [
              "hivemindd.exe",
              "hivemind.exe"
            ],
            "checkver": {
              "github": "https://github.com/causewayai/hivemind"
            }
          }
          EOF
          cd bucket
          git config user.name "causeway-release-bot"
          git config user.email "actions@github.com"
          # Renamed hivemindd.json -> hivemind.json (now ships both binaries).
          git rm -f bucket/hivemindd.json 2>/dev/null || true
          git add bucket/hivemind.json
          git commit -m "hivemind ${VERSION}"
          git push
